const PAGE_404 = "/404"
const PAGE_LIST = "/"

const URL = 'http://localhost:5000/jurassicpark/dinosaurs'

const MAIN_URL = `${URL}`
const URL_BY_ID = `${URL}/`

export { PAGE_404, PAGE_LIST, URL, MAIN_URL, URL_BY_ID}